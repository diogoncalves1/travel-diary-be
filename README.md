# travel-diary-be

## Environment Variables

This project uses Prisma ORM, which requires a database connection string.

You must create (or update) a `.env` file in the root of the backend project with the following variable:

```env
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME"
```
